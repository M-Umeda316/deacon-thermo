# CLAUDE.md

このリポジトリで作業するときに最初に読むファイル。コードよりもここに書いてある
**物理の枠組み**のほうが失われやすく、かつ価値が高い。

## このプロジェクトが答えようとしていること

Cu-K-Ln/γ-Al₂O₃ 系の HCl 酸化（Deacon）触媒について:

1. 反応条件下でどの凝縮相が安定か（Ln は塩化物のままか、オキシ塩化物になるか）
2. Cu の揮発速度はどれくらいか、K/Ln の添加がそれをどれだけ抑えるか
3. Ln を変えると 1, 2 がどう動くか

参照系: Cu-K-Sm/γ-Al₂O₃, 25-30 wt% SmCl₃, 380 ℃, HCl/O₂ = 2:1
（前身の Cu-K-La/γ-Al₂O₃ は 340 ℃ で HCl 転化率 78% を 9600 h 維持と報告）

## 絶対に忘れてはいけない前提

**この系は塩化物の三元系ではない。** CuCl₂-KCl-SmCl₃ の三元相図を描こうとすると
行き詰まる。実体は **Cu-K-Sm-Cl-O-H 系**であり、独立変数として塩素ポテンシャルと
酸素ポテンシャルが入る。

**CuCl₂ ⇌ CuCl + ½Cl₂ は障害ではなく反応機構そのもの。** Cu²⁺/Cu⁺ の酸化還元が
触媒サイクルなので、「CuCl₂ が分解して困る」という枠組みは間違い。

**p(Cl₂) は独立変数ではなく従属量。** 温度と供給 HCl/O₂ 比と転化率を決めれば
気相平衡から決まる。したがって `gas.py` が全ての計算の入口になる。

**Sm(II) は考えなくてよい。** Sm は 2 価が取れる Ln の一つだが、Deacon の
酸化雰囲気では届かない。Eu(II), Ce(IV), Pr(IV), Tb(IV) は届く。

**主たる失活経路は Cu の揮発。** 相図よりも先に揮発性を見るべき理由がこれ。
KCl / LnCl₃ を入れる主目的は Cu 塩化物の活量を下げること。

## 決着したこと

**Sm の相問題は決着（2026-08-09）。** Koch & Cunningham（JACS 75 (1953) 796、
加水分解平衡の直接実測 700-900 K）の転記データで、モデルの dG_hyd が反応温度
653 K で実測と 0.1 kJ/mol 一致（Gd も同様）。**SmCl₃ が安定なのは転化率 ~23%
まで。床の大半と出口は SmOCl 側（出口余裕 -23 kJ/mol）。** LnOCl は元素別
パラメータ（data.py の LNOCL_PARAMS。Koch 3 元素 + Yang 2022 熱量測定 6 元素）に
移行済み。**出口条件で塩化物側に残るのは La のみ（+5.5 kJ/mol とぎりぎり）。
Pr は実測反映で酸塩化物側（-9）に反転、Ce も内挿値で境界上（-1、どのみち
CeO₂ に行く）。** 系列余裕はイオン半径に対して単調（La +5.5 → Er -56）。
文献では La 系で LaOCl・LaCl₃・中間塩素化度の相がいずれも活性と報告されており、
オキシ塩化物は必ずしも失活相ではない。

**複塩仮説は熱力学的に成立（2026-08-10）。** Seifert 2002 Table 8 の K-Ln 複塩
19 種を data.py に登録（K_LN_DOUBLE_SALTS）し、出口ポテンシャルでの Cu:K:Sm
カチオン組成断面が 14 領域に分割されることを確認。**K が十分ある組成では Sm は
K₃SmCl₆ / K₂SmCl₅（塩化物複塩）として保持され、単一金属では SmOCl だった判定が
組成依存で反転する。** 参照組成（Cu:K:Sm ≈ 0.4:0.35:0.25）では Sm の約 7 割が
K₂SmCl₅、3 割が SmOCl。K₃SmCl₆ の安定下限は 606 K（333 ℃）で操業温度 380 ℃ は
安定域内（Nd は 714 K なので域外 — この差も K-Ln 複塩の系列トレンド）。
つまり **KCl 添加の熱力学的役割は「Cu 活量低下」だけでなく「Sm を塩化物として
係留する」こと**。注意: 融体相は候補に無いので Cu/K-rich 側は固体近似
（K₂CuCl₃ は 241 ℃ 以上で分解するため登録はあるが実相は融体）。

## 現時点で未決着なこと

**「系列内の差は絶対値より信頼できる」は共通モード誤差に限る。** 実測 DH_i は
-35〜-64 kJ/mol と元素でばらつく（sensitivity.py の掃引は共通モードのみを表現）。
全 11 元素が元素別値を持つ: 実測 8（Koch 5 元素 [La-Gd、手法統一] + Yang 3）、
比-内挿 3（Ce/Eu/Dy、Burns 1998 Fig.2 の方法論を自前データで再実装、±3 kJ/mol。
Eu は Burns 実測のオフセット補正値と 1.6 kJ/mol で一致し、旧 Dy の系列順位
アーティファクトも解消済み）。残る不整合は **La の Koch vs Yang 13 kJ/mol**
（Nd/Sm/Gd では両系統が 0.1〜2.3 kJ/mol で一致しており La だけが異常。
反応温度域を直接測る Koch を採用中）と、**Y の dS が Gibson 系フレーム**
（磁気エントロピーの扱いが Koch 系と異なる）の 2 点。

**旧「理想 Temkin が寿命を 2 桁外す」問題は Cu₃Cl₃(g) データ誤りが主因だった。**
JANAF 値（-258.571 / 429.526）への差し替えで解消し、xfail は通常テストに昇格済み。
差し替え後の反応条件での支配蒸気種は CuCl₂(g)。その **S°/Cp は分光定数からの
統計力学計算で確定済み**（S°₂₉₈ = 285.5 ± 4、docs/cucl2g_statmech.md。CuCl(g) で
コード検証済み）。残る未知は ΔHf₂₉₈ のみで、実測寿命 9600 h（Feng 2015、
**実条件は GHSV 450** — ReactorSpec 既定の 6000 は設計点であり混同しないこと）
からの下限 ≥ -52〜-49 に対し現行 -43 (EST) は整合。一次資料は
Wächter & Schäfer, ZAAC 471 (1980) 38 が唯一の手がかり。

**融液の非理想性は較正済み（ドライラン段階）。** Fontana 1952（30 mol% KCl の
平衡塩素圧 33 点）+ Niazi 2022（KCl-CuCl の CALPHAD、W(CuCl,KCl) の固定に使用）で
W(CuCl₂,KCl) - W(CuCl,KCl) = **+40 kJ/mol** が確定（RMS ×1.24）。つまり
**KCl は Cu(I) を Cu(II) より強く安定化する**（K₂CuCl₃ の存在と整合。
「クロロ銅酸錯体が Cu(II) を安定化して揮発を抑える」という旧仮説は逆向きだった）。
参照条件の寿命は理想 11,900 h / 較正後 6,900 h で、実測「≥9600 h」を挟む。
W(Cu-LnCl₃) は未較正（Ruthven & Kenney, JINC 30 (1968) 931 の四元系データ待ち）。
較正値の melt.py への組み込みは R&K 入手後にまとめて行う
（記録: docs/literature/extracted/calibration_dryrun_2026-08-09.md、ローカル）。

## データの扱い方（最重要）

計算結果は `data.py` の数字にぶら下がっている。結論を直接左右する残りの急所は
**CuCl₂(g) の ΔHf₂₉₈ だけ**になった（支配蒸気種。S°/Cp は統計力学で確定済み、
ΔHf は EST で寿命下限 ≥ -52 と整合）。SmOCl は Koch の直接実測で決着済み、
Cu₃Cl₃(g) は JANAF で確定済み。
2026-08 の文献調査報告（要取得リスト付き）が `docs/literature/` にある。

**有料文献の扱い（機密方針、2026-08-09）:** スキャン・PDF は AI に読ませず、
必要な数値はユーザーが紙面から転記する（依頼リスト = docs/literature/wanted_data.md、
転記形式の見本 = 過去の extracted/ ファイル）。転記した表は
`docs/literature/extracted/`（git 管理外）に置き、リポジトリに入れてよいのは
**出典・ページ付きの個別定数のみ**（data.py の LNOCL_PARAMS が例）。

- 全ての種に `confidence` と `source` を持たせてある。`DB.report()` で
  要検証のものが一覧できる。
- 数値を差し替えたら、**必ずコミットしてから図を再生成すること。**
  図とデータの対応が切れるのは熱力学計算で最悪の事故。`.gitignore` で
  `*.png` を除外しているのはこのため（図はデータのコミットハッシュと
  対応づけて別管理する）。
- 一次資料はページ番号まで `source` に書く。

**系列内の差は絶対値より信頼できる。** LnOCl の推定誤差は Ln 間で強く相関
するので、「Sm は塩化物か」には答えられなくても「La → Er でどちらに動くか」
には答えられる。単一元素の絶対判定を主張するときだけ一次資料が必須。

## Ln を変えるときの設計軸

混ぜると解釈できなくなるので分けて扱う。

- **軸A: Ln 自身の酸化還元** — Ce, Pr, Tb が +4、Eu が +2 に届く。
  第二の酸化還元中心になり、機構が変わる。Ce と Pr は酸化雰囲気で
  オキシ塩化物にならず CeO₂ / PrO₂ に行く。
- **軸B: イオン半径**（ランタノイド収縮）— 酸化還元不活性な
  La, Nd, Gd, Dy, Ho, Er, Y では、塩化物イオン活量・錯体安定性・
  液相線・Cu 活量係数を連続的に動かす。

**Y と Ho はほぼ同半径（0.900 / 0.901 Å）で Y は 4f を持たない。**
両者の性能が一致すれば効果は純粋にイオン半径・ルイス酸性、
違えば 4f が関与している、という決定実験になる。`radius_controls()` 参照。

記述子は n ≈ 15 なので GNN ではなく、イオン半径・加水分解 ΔG・
酸化還元フラグの 3 つで線形か GP を回すのが適切。

## コードの構成方針

```
species.py    Species / Database の型定義。物理量の計算はここだけ
data.py       数値。差し替えるのはここ。ロジックを書かない
gas.py        気相 Deacon 平衡。全ての計算の入口
stability.py  Kellogg 図と加水分解。候補相が有限なので argmin で済む
melt.py       融液活量。ActivityModel を継承すれば MQMQA に差し替え可能
sensitivity.py DH_OXYCHLORIDE の感度解析。順位の頑健性と反転閾値
assemblage.py 気相固定ポテンシャル下の多金属相集合（カチオン組成断面）を線形計画で解く
volatility.py Cu 蒸気圧と寿命
lanthanides.py 系列比較と記述子
```

汎用の Gibbs 最小化ソルバは意図的に使っていない。候補相が有限個なら
線形不等式問題なので numpy で足りる。pycalphad / Thermochimica が必要に
なるのは液相線を本気で計算する段階から。

## 作業の優先順位

1. 転記待ちの消化（docs/literature/wanted_data.md）: Koch III の Pr/Nd（P2-3）、
   Seifert レビューの K 系複塩表（P2-1/2-2、特に Sm）、Koch I/II の測定温度域
2. 未入手文献: Ruthven & Kenney JINC 30(1968)931 [W(Cu-LaCl₃) の唯一のデータ]、
   Wächter & Schäfer ZAAC 471(1980)38 [CuCl₂(g)]、Seifert & Büchel ZAAC 624
   (1998)342 [Y 複塩、Y/Ho 対照の片割れ]
3. 較正値（Fontana+Niazi の W）を melt.py の既定モデルに組み込み、
   揮発・寿命の系列予測を元素別 LNOCL_PARAMS と合わせて再生成
4. Ln 系列の記述子表を出し（複塩安定性・元素別 DH を記述子に追加）、
   実験計画（Nd, Gd, Ce, La, Y/Ho）に落とす
5. 必要なら液相線に進む（ここで初めて pycalphad + MQMQA。融体を
   assemblage の候補に入れると Cu/K-rich 側の固体近似も解消する）
